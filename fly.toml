# fly.toml app configuration file generated for dinodigest-worker on 2026-03-26T15:45:20+08:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'dinodigest-worker'
primary_region = 'nrt'

[build]
  dockerfile = 'Dockerfile.worker'

[env]
  NODE_ENV = 'production'

[processes]
  worker = ''

[[vm]]
  size = 'shared-cpu-1x'
  memory = '256mb'
  cpus = 1
  memory_mb = 256
